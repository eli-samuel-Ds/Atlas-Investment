@model Application.ViewModels.Country.CountryDeleteViewModel
@{
    ViewData["Title"] = "Eliminar País";
}
<div class="container d-flex justify-content-center mt-5">
    <div class="card shadow-sm w-100 text-center" style="max-width: 450px;">
        <div class="card-header bg-danger text-white">
            <h4 class="mb-0">Eliminar País</h4>
        </div>
        <div class="card-body">
            <p class="mb-4">¿Estás seguro que deseas eliminar este país?</p>
            <h5 class="fw-bold">@Model.Name (@Model.IsoCode)</h5>
            <form asp-action="Delete" method="post" class="mt-4">
                <input type="hidden" asp-for="Id" />
                <div class="d-flex justify-content-center gap-2">
                    <a asp-action="Paises" class="btn btn-outline-secondary">Cancelar</a>
                    <button type="submit" class="btn btn-danger">Aceptar</button>
                </div>
            </form>
        </div>
    </div>
</div>
